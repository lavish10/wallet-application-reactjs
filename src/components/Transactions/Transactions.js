import React from 'react';

const Transactions = () => {
    return (
        <div>
            <div className={'container'} style={{marginTop: '100px'}}>
                <h2>Transactions</h2>
            </div>
        </div>
    );
};

export default Transactions;